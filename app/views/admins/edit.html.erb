<%= render "helpers/bread", index: admins_path, iname: "Administradores", current: "Editar" %>
 <section class="card mb-3">    
	<div class="card-body">
		<div class="card-title mb-3">Editar Administrador</div>
	   	<%= render 'form', admin: @admin %>	   
	</div>
 </section>

 <section class="card mb-3">    
	<div class="card-body">
		<div class="card-title mb-3">Modificar contraseña</div>
	   	<%= render 'pass_form', admin: @admin %>
	</div>
 </section>