<%= render "helpers/bread", index: business_path, iname: "#{@business.name}", current: "Editar" %>
<section class="card">

  <div class="card-header py-3 px-3">
    <h4>
      <strong>Centro de trabajo</strong>
    </h4>
  </div>
  <% if @business.status == 1 %>  
    <div class="alert alert-warning mx-3 my-3" role="alert">
      Realizar algún cambio en los datos del centro de trabajo, requiere volver a validar.
      <%= link_to "Cancelar" , businesses_path , class: "text-danger" %>
    </div>
  <% end %>
  <div class="card-body px-5">
    <%= render 'form_edit', business: @business %>
  </div>
</section>


